export enum RoleType {
  SUPERADMIN = 'SUPERADMIN',
  ADMIN = 'ADMIN',
  MEMBER = 'MEMBER',
  VIEWER = 'VIEWER',
}

export enum FieldKind {
  String = 'String',
  Number = 'Number',
  DateTime = 'DateTime',
  Boolean = 'Boolean',
  Select = 'Select',
  UserRef = 'UserRef',
  RecordRef = 'RecordRef',
  Email = 'Email',
  Url = 'Url',
}
